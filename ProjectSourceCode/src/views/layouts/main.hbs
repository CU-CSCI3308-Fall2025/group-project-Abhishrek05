<!doctype html>
<html lang="en">
  <head>
    {{> head}}
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{title}}</title>
  </head>
  <body>
    {{#if user}}
      {{> navbar}}
    {{/if}}
    {{{body}}}
  </body>
  
</html>
