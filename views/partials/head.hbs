<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
<style>
  /* Light theme only */
  html, body { height: 100%; }
  body { font-family: Inter, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, sans-serif; margin: 0; color: #24292f; background: #f6f8fa; }
  .page { min-height: 100%; display: grid; place-items: center; padding: 24px; }
  .container { width: 100%; max-width: 960px; display: grid; grid-template-columns: 1fr 1fr; gap: 48px; align-items: start; }
  .card { width: 100%; background: #ffffff; color: #24292f; border: 1px solid #d0d7de; border-radius: 12px; box-shadow: 0 1px 2px rgba(27,31,36,0.06); padding: 24px; }
  h1 { margin: 0 0 6px; font-size: 36px; letter-spacing: 0.2px; }
  .subtitle { margin: 0 0 18px; color: #57606a; font-size: 14px; }
  .subtitle a { color: #0969da; text-decoration: none; }
  .subtitle a:hover { text-decoration: underline; }
  form { display: grid; gap: 16px; }
  label { display: block; margin-bottom: 0.25rem; font-weight: 600; }
  input { width: 100%; padding: 0.5rem 0.6rem; border: 1px solid #d0d7de; border-radius: 6px; background: #ffffff; color: #24292f; }
  .btn-primary { padding: 12px 16px; border: 1px solid #24292f; background: #24292f; color: #fff; border-radius: 8px; cursor: pointer; font-weight: 600; }
  .btn-primary:hover { background: #000; border-color: #000; }
  .field { display: grid; gap: 6px; }
  .field small { color: #57606a; }
  .actions { margin-top: 6px; }
  .label-sm { font-size: 11px; letter-spacing: 0.06em; color: #57606a; }

  /* Decorative sticky notes like the wireframe */
  .decor { position: relative; height: 420px; }
  .sticky { position: absolute; width: 170px; height: 160px; border-radius: 6px; box-shadow: 0 6px 0 rgba(0,0,0,0.08); border: 1px solid #d0d7de; display: grid; place-items: center; color: #24292f; }
  .sticky.gray { background: #bfbfbf; left: 20px; top: 120px; }
  .sticky.yellow { background: #f6d27b; left: 230px; top: 90px; }
  .sticky.pink { background: #f8c6cd; left: 120px; top: 300px; }
</style>
